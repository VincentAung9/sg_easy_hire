import 'package:flutter/widgets.dart';

class HelperInterviewsView extends StatelessWidget {
  const HelperInterviewsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("HELPER INTERVIEWS");
  }
}
