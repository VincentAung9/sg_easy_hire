import 'package:flutter/widgets.dart';

class HelperJobsView extends StatelessWidget {
  const HelperJobsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("HELPER JOBS");
  }
}
