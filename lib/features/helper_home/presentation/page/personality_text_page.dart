import 'package:flutter/widgets.dart';

class PersonalityTextPage extends StatelessWidget {
  const PersonalityTextPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
