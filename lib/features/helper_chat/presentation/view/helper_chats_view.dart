import 'package:flutter/widgets.dart';

class HelperChatsView extends StatelessWidget {
  const HelperChatsView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text("HELPER CHAT VIEW");
  }
}
